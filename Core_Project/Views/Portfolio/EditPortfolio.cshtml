@model EntityLayer.Concrete.Portfolio
@{
    ViewData["Title"] = "EditPortfolio";
    Layout = "~/Views/Shared/_NewAdminLayout.cshtml";
}

@using (Html.BeginForm("EditPortfolio", "Portfolio", FormMethod.Post))
{
    <div class="col-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <form class="forms-sample">
                    <div class="form-group">
                        @Html.HiddenFor(x=>x.PortfolioId, new {@class = "form-control"})
                    </div>
                    <div class="form-group">
                        <label>Proje Adı</label>
                        @Html.TextBoxFor(x=>x.PortfolioName, new {@class = "form-control"})
                        @Html.ValidationMessageFor(x=>x.PortfolioName)
                    </div>
                    <div class="form-group">
                        <label>Küçük Resim Yolu</label>
                        @Html.TextBoxFor(x=>x.PortfolioImageUrl, new {@class = "form-control"})
                        @Html.ValidationMessageFor(x=>x.PortfolioImageUrl)
                    </div>
                    <div class="form-group">
                        <label>Büyük Resim Yolu</label>
                        @Html.TextBoxFor(x=>x.PortfolioImageUrl2, new {@class = "form-control"})
                        @Html.ValidationMessageFor(x=>x.PortfolioImageUrl2)
                    </div>
                    <div class="form-group">
                        <label>Proje URL</label>
                        @Html.TextBoxFor(x=>x.PortfolioUrl, new {@class = "form-control"})
                        @Html.ValidationMessageFor(x=>x.PortfolioUrl)
                    </div>
                    <div class="form-group">
                        <label>Platform</label>
                        @Html.TextBoxFor(x=>x.PortfolioPlatform, new {@class = "form-control"})
                        @Html.ValidationMessageFor(x=>x.PortfolioPlatform)
                    </div>
                    <div class="form-group">
                        <label>Ücret</label>
                        @Html.TextBoxFor(x=>x.PortfolioPrice,new {@class = "form-control"})
                        @Html.ValidationMessageFor(x=>x.PortfolioPrice)
                    </div>
                    <div class="form-group">
                        <label>İlerleme Durumu</label>
                        @Html.TextBoxFor(x=>x.PortfolioRatio,new {@class = "form-control"})
                        @Html.ValidationMessageFor(x=>x.PortfolioRatio)
                    </div>
                    <button type="submit" class="btn btn-primary mr-2">Güncelle</button>
                    <a href="/Portfolio/Index" class="btn btn-dark">Geri</a>
                </form>
            </div>
        </div>
    </div>
}

